<phone:PhoneApplicationPage
    x:Class="WritersToolbox.views.PivotPage1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait"  Orientation="Portrait"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot ist das Stammraster, in dem alle anderen Seiteninhalte platziert werden-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <!--Pivotsteuerelement-->
        <phone:Pivot Title="MEINE ANWENDUNG">
            <!--Pivotelement eins-->
            <phone:PivotItem Header="Bilder" Margin="12,28,12,102">
                <Grid>
                    <Button Content="+" HorizontalAlignment="Left" Height="127" Margin="10,10,0,0" VerticalAlignment="Top" Width="436" FontSize="22"/>
                </Grid>
            </phone:PivotItem>

            <!--Pivotelement zwei-->
            <phone:PivotItem Header="Notiz" Margin="12,28,12,102">
                <Grid Grid.Row="1" Margin="0">
                    <TextBox HorizontalAlignment="Left" Height="74" Margin="22,25,0,0" TextWrapping="Wrap" Text="Titel" VerticalAlignment="Top" Width="410"/>

                    <!--<TextBox Opacity="1.0" HorizontalAlignment="Left" Height="387" Margin="22,104,0,0" TextWrapping="Wrap" Text="Details" VerticalAlignment="Top" Width="410" VerticalScrollBarVisibility="Visible" AcceptsReturn="True" HorizontalScrollBarVisibility="Visible"/>-->

                    <UserControl>
                        <Grid x:Name="LayoutRoot12" Background="Transparent">
                            <ScrollViewer x:Name="scrollViewer" Width="{Binding ActualWidth, ElementName=userControl, Mode=OneWay}" Height="{Binding ActualHeight, ElementName=userControl, Mode=OneWay}">
                                <TextBox TextWrapping="Wrap" Text="Add some pretty long text here..." Height="333"/>
                            </ScrollViewer>
                        </Grid>
                    </UserControl>
                </Grid>
            </phone:PivotItem>
        </phone:Pivot>
        <Button x:Name="saveButton" Content="S" HorizontalAlignment="Left" Height="75" Margin="10,683,0,0" VerticalAlignment="Top" Width="145"/>
        <Button x:Name="saveAsButton" Content="Sas" HorizontalAlignment="Left" Height="75" Margin="176,683,0,0" VerticalAlignment="Top" Width="145"/>
        <Button x:Name="closeButton" Content="C" HorizontalAlignment="Left" Height="75" Margin="326,683,0,0" VerticalAlignment="Top" Width="145"/>

    </Grid>

</phone:PhoneApplicationPage>