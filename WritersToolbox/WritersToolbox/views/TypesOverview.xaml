<phone:PhoneApplicationPage
    x:Class="WritersToolbox.views.TypesOverview"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:wt="clr-namespace:WritersToolbox.viewmodels"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="True">
    
    <phone:PhoneApplicationPage.Resources>
        <DataTemplate x:Key="TypesOverviewDataTemplate">
            <Grid>
                <Rectangle Width="Auto" Height="Auto"/>
                <TextBlock Text="{Binding Strings}"/>
            </Grid>
        </DataTemplate>
        
    </phone:PhoneApplicationPage.Resources>

    <!--LayoutRoot ist das Stammraster, in dem alle anderen Seiteninhalte platziert werden-->
    <Grid x:Name="LayoutRoot" d:DataContext="{d:DesignInstance Type=wt:AddNoteViewModel, IsDesignTimeCreatable=true}" Background="White">
        <Grid>
            <Grid.Background>
                <ImageBrush Stretch="Fill" ImageSource="/Assets/start_background.png" Opacity="0.6"/>
            </Grid.Background>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Rectangle Fill="#FF72A91C" Margin="0,0,0,0" VerticalAlignment="Top" Height="50"></Rectangle>
            <Rectangle Grid.Row="1" Fill="#FF72A91C" Margin="0,0,0,0" VerticalAlignment="Bottom" Height="50"></Rectangle>


            <!--TitlePanel enthält den Namen der Anwendung und den Seitentitel-->
            <StackPanel Grid.Row="0" Margin="12,17,0,28">
                <TextBlock Text="Typen" Style="{StaticResource PhoneTextNormalStyle}"/>
                <TextBlock Text="Überblick" Margin="9,10,0,0" Style="{StaticResource PhoneTextTitle1Style}" Foreground="Black" FontSize="50" Height="65"/>
            </StackPanel>

            <!--ContentPanel - zusätzliche Inhalte hier platzieren-->
            <Grid x:Name="ContentPanel" Grid.Row="1" Margin="12,0,12,0" ManipulationDelta="pinch_out">
                <phone:LongListSelector x:Name="longListTypes" Height="Auto" Margin="0,0,0,0" Width="Auto" GridCellSize="150, 150" ItemTemplate="{StaticResource TypesOverviewDataTemplate}" ItemsSource="{Binding String}">
                   
                </phone:LongListSelector>

            </Grid>
        </Grid>
    </Grid>

</phone:PhoneApplicationPage>