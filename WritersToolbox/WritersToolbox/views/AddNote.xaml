<phone:PhoneApplicationPage
    x:Class="WritersToolbox.views.AddNote"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot ist das Stammraster, in dem alle anderen Seiteninhalte platziert werden-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--TitlePanel enthält den Namen der Anwendung und den Seitentitel-->
        <StackPanel Grid.Row="0" Margin="12,17,0,28">
            <TextBlock Text="neue Notiz anlegen" Style="{StaticResource PhoneTextNormalStyle}"/>
        </StackPanel>

        <!--ContentPanel - zusätzliche Inhalte hier platzieren-->
        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="12,0,12,0">
            <Canvas x:Name="canvasNote" HorizontalAlignment="Left" Height="485" Margin="10,128,0,0" VerticalAlignment="Top" Background="Transparent" Width="436" />
            <TextBox x:Name="detailsNote" TextWrapping="Wrap" Text="details" Width="436" BorderBrush="#33FFFFFF" Background="{StaticResource TransparentBrush}" Foreground="#FFD7F709" Margin="10,128,10,83" ManipulationDelta="detailsNote_ManipulationDelta" VerticalScrollBarVisibility="Auto" InputScope="Text" Tap="detailsNote_Tap" GotFocus="detailsNote_GotFocus"/>
            <TextBox x:Name="titleNote" HorizontalAlignment="Left" Height="72" TextWrapping="Wrap" Text="title" VerticalAlignment="Top" Width="436" Background="Transparent" Foreground="#FFFBFBFB" BorderBrush="#33FFFFFF"/>
        </Grid>
        <Button x:Name="saveButton" Content="S" HorizontalAlignment="Left" Height="74" VerticalAlignment="Top" Width="86" Margin="104,610,0,0" Grid.Row="1" Click="saveButton_Click"/>
        <Button x:Name="saveAsButton" Content="Sas" HorizontalAlignment="Left" Height="74" VerticalAlignment="Top" Width="86" Margin="195,610,0,0" Grid.Row="1"/>
        <Button x:Name="closeButton" Content="C" HorizontalAlignment="Left" Height="74" VerticalAlignment="Top" Width="86" Margin="278,610,0,0" Grid.Row="1"/>
        <ContentControl Content="ContentControl" HorizontalAlignment="Left" VerticalAlignment="Top"/>
    </Grid>

</phone:PhoneApplicationPage>